function aggiungiPreferito(id_evento, iconaPreferiti) {
    /* const iconaPreferiti = document.querySelector("#preferiti"); */
    iconaPreferiti.src = "/pictures/preferito-attivo.png"; // Cambia l'icona del cuore preferiti
    // Invia una richiesta al server per salvare l'evento preferito nel database
    const xhr = new XMLHttpRequest();
    xhr.open("POST", "/api/preferiti");
    xhr.setRequestHeader("Content-Type", "application/json");
    xhr.onload = function () {
        if (xhr.status === 200) {
            // Gestisci la risposta dal server se necessario
        } else {
            console.error("Errore durante il salvataggio dell'evento preferito:", xhr.statusText);
        }
    };
    xhr.onerror = function () {
        console.error("Errore di rete durante la richiesta.");
    };
    xhr.send(JSON.stringify({ id_evento: id_evento }));
}



